import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Amollu Services | Property Care & Facility Solutions",
  description:
    "Residential, commercial, and facility property care services from Amollu Services.",
  openGraph: {
    title: "Amollu Services",
    description:
      "Reliable residential, commercial, and facility property care services.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={geistSans.variable}>
      <body>{children}</body>
    </html>
  );
}
